<UserControl x:Class="Check.SPort.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Check.SPort.View"
             xmlns:vm="clr-namespace:Check.SPort.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="725"
             d:DataContext="{d:DesignInstance vm:MainViewModel}"
             Style="{StaticResource Page_Style}">
    
    <Grid>

        <Border CornerRadius="0,20,20,0">
            <Border.Background>
                <ImageBrush ImageSource="\Resources\Image\sfondo_001.jpg" Stretch="UniformToFill"/>
            </Border.Background>
        </Border>

    </Grid>
</UserControl>
